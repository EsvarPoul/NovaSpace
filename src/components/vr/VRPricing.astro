---
import { vrPricing } from "../../data/vr.js";
import Section from "../ui/Section.astro";
---

<Section id="pricing" eyebrow="Формати та вартість" title="М'яка сітка цін для VR, PS5, кімнат і свят." className="vr-app-section vr-presets">
  <div class="vr-price-board">
    {vrPricing.map((group: any) => (
      <article class="vr-price-card">
        <div>
          <span>{group.note}</span>
          <strong>{group.title}</strong>
        </div>
        <table>
          <thead>
            <tr>
              <th>Формат</th>
              {group.columns.map((column: string) => <th>{column}</th>)}
            </tr>
          </thead>
          <tbody>
            {group.rows.map((row: any) => (
              <tr>
                <th>{row.label}</th>
                {row.values.map((value: string) => <td>{value}</td>)}
              </tr>
            ))}
          </tbody>
        </table>
      </article>
    ))}
  </div>
</Section>
